# KVN Bridge Master

Bash-скрипт для автоматического развертывания и управления VPN-инфраструктурой на базе Xray (VLESS-XTLS-Reality / xHTTP).

## 🛠 Функциональность

* **Архитектура:** Входной сервер (Bridge) принимает подключения и балансирует их между удаленными нодами (Exit Nodes).
* **Клиентский UX:**
  * Генерация персональных HTML-страниц для каждого пользователя.
  * Поддержка **Deep Links** (автоматическая настройка приложений Hiddify, V2Box, V2RayTun в один клик).
  * URL-подписки для автоматического обновления конфигураций на клиентах.
* **Telegram Бот (Go):**
  * Написан на Go (минимальное потребление ресурсов, работа в виде systemd-сервиса).
  * Управление пользователями, генерация ссылок, мониторинг ресурсов, создание бекапов.
  * Управление SNI (маскировочными доменами) на удаленных нодах без правки конфигов.
* **Маскировка:** Nginx с валидным SSL (Let's Encrypt) и имитацией страницы Zabbix/авторизации.
* **Маршрутизация:**
  * Автоматическая настройка **Cloudflare WARP** на выходах для обхода блокировок по IP.
  * Возможность редактирования правил маршрутизации WARP через меню.
* **Раздельное туннелирование:** Автоматическое развертывание панели **SSAntifilter** (Docker) от [Zerolab](https://zerolab.net/super-simple-antifilter/) для генерации списков обхода блокировок.
* **Бекап и миграция:**
  * Полное резервное копирование (конфиги, ключи, база пользователей, Docker-контейнеры).
  * Функция восстановления с **автоматической сменой домена** (на случай блокировки) и перегенерацией всех клиентских подписок.
* **Безопасность:** Настройка UFW, Fail2Ban, смена SSH-порта, отключение парольного входа.

## 💻 Системные требования

* **ОС:** Ubuntu 22.04+ / Debian 11+ (Проверено на Debian 12).
* **Права:** `root`.
* **Ресурсы:** От **1 GB RAM** (требуется для сборки Go-бота и работы Docker).
* **Серверы:**
  * 1 сервер (Bridge) в качестве точки входа — обязательна привязка домена (A-запись).
  * 1+ удаленных серверов (Exit Nodes).

## 🚀 Установка

Выполните команды на сервере, который будет точкой входа (Bridge):

```bash
wget -O bridge.sh https://raw.githubusercontent.com/sud0-i/BridgeMaster/main/bridgeStable.sh
chmod +x bridge.sh
./bridge.sh
```

## Первый запуск

1. В главном меню выберите **1) 🌍 ИНФРАСТРУКТУРА** -> **1) Развернуть новый мост**.
2. Следуйте инструкциям мастера настройки (ввод домена, IP удаленного сервера, пароля root).
   > Скрипт настроит авторизацию по SSH-ключам между серверами. Парольный вход на удаленной ноде будет отключен.
3. По завершении вы получите:
   * Ссылку на панель управления правилами: `https://ваш-домен/antifilter/`.
   * Пароль администратора от панели.
   * VLESS-ссылку администратора.

## Управление через Telegram Бота

Бот устанавливается через пункт меню **9) 🤖 Telegram Бот**. Скрипт скачивает компилятор Go, собирает бинарный файл под архитектуру сервера и регистрирует systemd-сервис.

**Кнопки меню у аккаунта администратора:**
* **Статус:** Загрузка CPU/RAM и доступность удаленных нод.
* **Трафик:** Статистика потребления по пользователям (сбрасывается при рестарте Xray).
* **Клиенты:** Добавление/удаление клиентов, создание инвайтов для тг бота.
* **Управление:** Смена SNI на нодах, рестарт кластера, формирование бекапа.

**Кнопки меню у аккаунта клиента:**
* **Статус:** Состояние доступности серверов.
* **Моя ссылка:** Ссылка на html страницу с мануалом по настройке.

## Структура файлов

* `/usr/local/etc/xray/config.json` — Основной конфиг маршрутизации.
* `/var/www/html/sub/` — Директория с файлами подписок и HTML-страницами клиентов.
* `/usr/local/bin/vpn-bot` — Исполняемый файл бота.
* `/root/.vpn_tg.conf` — Токен и настройки бота.

## Поддерживаемые клиенты

Сгенерированные ссылки и Deep Links протестированы в приложениях:
* **iOS:** V2RayTun.
* **Android:** Hiddify, V2RayTun, v2rayNG.
* **PC/Mac:** Hiddify Next, Throne.

## Отказ от ответственности

Данный проект предоставляется «как есть» исключительно в образовательных и исследовательских целях. Автор не несет ответственности за любой ущерб или неправомерное использование данного программного обеспечения.
